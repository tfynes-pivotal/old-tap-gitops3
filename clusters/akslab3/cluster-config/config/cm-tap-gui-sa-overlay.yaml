apiVersion: v1
kind: Secret
metadata:
  name: cm-tap-gui-sa-overlay
  namespace: tap-install
stringData:
  cm-tap-gui-sa-overlay.yaml: |
    #@ load("@ytt:overlay", "overlay")
    #@overlay/match by=overlay.subset({"metadata":{"name":"tap-gui","namespace":"tap-gui"},"kind":"ServiceAccount"}), expects="0+"
    ---
    #@overlay/append
    imagePullSecrets:
    - name: private-registry-credentials